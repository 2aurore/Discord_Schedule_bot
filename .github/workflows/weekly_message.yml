name: Weekly Discord Message

on:
  schedule:
    # 매주 월요일 오전 9시 (UTC 기준, 한국 시간은 18:00)
    # cron: '분 시 일 토 요일'
    - cron: '0 9 * * 6'
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  send_message:
    runs-on: ubuntu-latest
    steps:
      - name: Get Current Date and Day
        id: date
        run: |
          # 한국 시간(KST) 기준으로 실행일(토요일)로부터 '다음 수요일' 계산
          # 'next wednesday' 옵션을 사용하면 가장 가까운 미래의 수요일을 가져옵니다.
          TARGET_DATE=$(TZ='Asia/Seoul' date -d "next wednesday" +'%Y-%m-%d')
          
          # 환경 변수에 저장 (수요일이므로 요일은 '수'로 고정)
          echo "formatted_date=${TARGET_DATE} 수" >> $GITHUB_ENV

      - name: Create Forum Thread
        run: |
          curl -X POST "${{ secrets.DISCORD_WEBHOOK_URL }}?wait=true" \
            -H "Content-Type: application/json" \
            -d '{
              "thread_name": "test : [일정 공지] ${{ env.formatted_date }}",
              "content": 📅 <@&1456113177937969381> **돌아오는 주 일정 확인해주세요!**\n\n> 일정 변경이 필요한 경우 본 포스트의 댓글을\n> **월요일 오후 6시**까지 남겨주세요 <:FKK_D_03:1456136749968265310>\n\n이후 급한 변경 사항은 별도로 연락 부탁드립니다!\n\n**📌 기존 정기 일정**\n- 매주 수요일 오후 9시 30분"
            }'
